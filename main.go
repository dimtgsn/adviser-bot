package main

func main() {
	// token = flags.Get(token)

	// thClient = telegram.New(token)

	// fetcher = fetcher.New()

	// processor = processor.New()

	// consumer = consumer.Start(fetcher, processor)
}
